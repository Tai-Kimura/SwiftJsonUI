# HotLoader Configuration
# This file allows you to configure which Info.plist file to use for HotLoader IP settings

# Source directory (from config.json)
# This will be populated automatically from config.json
SOURCE_DIR=""

# Path to Info.plist (relative to source directory)
# Default: Info.plist
# You can customize this to match your project structure
PLIST_PATH="Info.plist"

# HotLoader server directory (relative to project directory)
# Default: hot_loader
HOTLOADER_DIR="hot_loader"