// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang swift

import Foundation

/// Typed attribute extraction for the `ScrollView` component.
/// Shared attributes are available via `common`.
public struct ScrollViewAttributes {
    public enum ContentInsetAdjustmentBehavior: String {
        case never = "never"
        case always = "always"
        case automatic = "automatic"
        case scrollableAxes = "scrollableAxes"
    }

    public enum Orientation: String {
        case horizontal = "horizontal"
        case vertical = "vertical"
    }

    public enum ScrollBehavior: String {
        case auto = "auto"
        case smooth = "smooth"
    }

    public enum ScrollMode: String {
        case inner = "inner"
        case window = "window"
    }

    /// Canonical attribute names declared for this component, including the shared `common` set (public metadata contract).
    public static let declaredAttributes: Set<String> = CommonAttributes.declaredAttributes.union([
        "bounces",
        "child",
        "children",
        "contentInsetAdjustmentBehavior",
        "contentOffset",
        "contentSize",
        "decelerationRate",
        "indicatorStyle",
        "keyboardAvoidance",
        "keyboardDismissMode",
        "maxZoom",
        "minZoom",
        "orientation",
        "paging",
        "scrollBehavior",
        "scrollEnabled",
        "scrollMode",
        "scrollsToTop",
        "showsHorizontalScrollIndicator",
        "showsVerticalScrollIndicator",
    ])

    /// Alias spelling → canonical attribute name (merged with common). Alias
    /// spellings that are also declared attributes keep their own
    /// entry and are not redirected.
    public static let aliasMap: [String: String] = [:]

    /// True when `key` is a declared canonical name or alias spelling.
    public static func isDeclared(_ key: String) -> Bool {
        return declaredAttributes.contains(key) || aliasMap[key] != nil
    }

    /// Attributes shared across all components.
    public let common: CommonAttributes

    /// Enable bounce effect
    public let bounces: Bool?

    /// Child component(s)
    public let child: [Any]?

    /// Child components
    public let children: [Any]?

    /// Content inset adjustment
    public let contentInsetAdjustmentBehavior: AttrEnum<ContentInsetAdjustmentBehavior>?

    /// Initial offset [x, y]
    public let contentOffset: [Any]?

    /// Content size [width, height]
    public let contentSize: [Any]?

    /// Deceleration rate
    public let decelerationRate: String?

    /// Scroll indicator style
    public let indicatorStyle: String?

    /// Enable keyboard avoidance
    public let keyboardAvoidance: Bool?

    /// Keyboard dismiss mode
    public let keyboardDismissMode: String?

    /// Maximum zoom scale (binding supported)
    public let maxZoom: AttrValue<Double>?

    /// Minimum zoom scale (binding supported)
    public let minZoom: AttrValue<Double>?

    /// Scroll orientation
    public let orientation: AttrEnum<Orientation>?

    /// Enable paging
    public let paging: Bool?

    /// Scroll behavior animation
    public let scrollBehavior: AttrEnum<ScrollBehavior>?

    /// Enable scrolling (binding supported)
    public let scrollEnabled: AttrValue<Bool>?

    /// Where the scroll happens. 'inner' (default, iOS/Android-style): the ScrollView div itself gets overflow-y-auto and scrolls internally — requires a bounded-height ancestor on web to actually activate. 'window' (web-only escape hatch): skip the overflow class so the page/window scrolls naturally; fixes position:sticky descendants that would otherwise be anchored to a never-scrolling inner container. iOS/Android ignore this attribute (UIScrollView always scrolls internally).
    public let scrollMode: AttrEnum<ScrollMode>?

    /// Scroll to top on status bar tap
    public let scrollsToTop: Bool?

    /// Show horizontal indicator
    public let showsHorizontalScrollIndicator: Bool?

    /// Show vertical indicator
    public let showsVerticalScrollIndicator: Bool?

    /// Pass `canonicalOnly: true` for L1-normalized input —
    /// alias fallback is then disabled.
    public init(json: [String: Any], canonicalOnly: Bool = false) {
        self.common = CommonAttributes(json: json, canonicalOnly: canonicalOnly)
        self.bounces = AttrCoerce.boolean(AttrCoerce.lookup(json, "bounces"))
        self.child = AttrCoerce.array(AttrCoerce.lookup(json, "child"))
        self.children = AttrCoerce.array(AttrCoerce.lookup(json, "children"))
        self.contentInsetAdjustmentBehavior = Self.parseContentInsetAdjustmentBehavior(AttrCoerce.lookup(json, "contentInsetAdjustmentBehavior"))
        self.contentOffset = AttrCoerce.array(AttrCoerce.lookup(json, "contentOffset"))
        self.contentSize = AttrCoerce.array(AttrCoerce.lookup(json, "contentSize"))
        self.decelerationRate = AttrCoerce.string(AttrCoerce.lookup(json, "decelerationRate"))
        self.indicatorStyle = AttrCoerce.string(AttrCoerce.lookup(json, "indicatorStyle"))
        self.keyboardAvoidance = AttrCoerce.boolean(AttrCoerce.lookup(json, "keyboardAvoidance"))
        self.keyboardDismissMode = AttrCoerce.string(AttrCoerce.lookup(json, "keyboardDismissMode"))
        self.maxZoom = AttrCoerce.attrValue(AttrCoerce.lookup(json, "maxZoom"), AttrCoerce.number)
        self.minZoom = AttrCoerce.attrValue(AttrCoerce.lookup(json, "minZoom"), AttrCoerce.number)
        self.orientation = Self.parseOrientation(AttrCoerce.lookup(json, "orientation"))
        self.paging = AttrCoerce.boolean(AttrCoerce.lookup(json, "paging"))
        self.scrollBehavior = Self.parseScrollBehavior(AttrCoerce.lookup(json, "scrollBehavior"))
        self.scrollEnabled = AttrCoerce.attrValue(AttrCoerce.lookup(json, "scrollEnabled"), AttrCoerce.boolean)
        self.scrollMode = Self.parseScrollMode(AttrCoerce.lookup(json, "scrollMode"))
        self.scrollsToTop = AttrCoerce.boolean(AttrCoerce.lookup(json, "scrollsToTop"))
        self.showsHorizontalScrollIndicator = AttrCoerce.boolean(AttrCoerce.lookup(json, "showsHorizontalScrollIndicator"))
        self.showsVerticalScrollIndicator = AttrCoerce.boolean(AttrCoerce.lookup(json, "showsVerticalScrollIndicator"))
    }

    private static func parseContentInsetAdjustmentBehavior(_ raw: Any?) -> AttrEnum<ContentInsetAdjustmentBehavior>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "never": return .known(ContentInsetAdjustmentBehavior.never)
            case "always": return .known(ContentInsetAdjustmentBehavior.always)
            case "automatic": return .known(ContentInsetAdjustmentBehavior.automatic)
            case "scrollableaxes": return .known(ContentInsetAdjustmentBehavior.scrollableAxes)
            default: break
            }
        }
        AttrCodegenWarnings.emit("ScrollView.contentInsetAdjustmentBehavior: unknown enum value '\(raw)'")
        return .unknown(raw)
    }

    private static func parseOrientation(_ raw: Any?) -> AttrEnum<Orientation>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "horizontal": return .known(Orientation.horizontal)
            case "vertical": return .known(Orientation.vertical)
            default: break
            }
        }
        AttrCodegenWarnings.emit("ScrollView.orientation: unknown enum value '\(raw)'")
        return .unknown(raw)
    }

    private static func parseScrollBehavior(_ raw: Any?) -> AttrEnum<ScrollBehavior>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "auto": return .known(ScrollBehavior.auto)
            case "smooth": return .known(ScrollBehavior.smooth)
            default: break
            }
        }
        AttrCodegenWarnings.emit("ScrollView.scrollBehavior: unknown enum value '\(raw)'")
        return .unknown(raw)
    }

    private static func parseScrollMode(_ raw: Any?) -> AttrEnum<ScrollMode>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "inner": return .known(ScrollMode.inner)
            case "window": return .known(ScrollMode.window)
            default: break
            }
        }
        AttrCodegenWarnings.emit("ScrollView.scrollMode: unknown enum value '\(raw)'")
        return .unknown(raw)
    }
}
