// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang swift

import Foundation

/// Typed attribute extraction for the `Progress` component.
/// Shared attributes are available via `common`.
/// Overrides the common definition of: `tintColor` (use the property on this struct).
public struct ProgressAttributes {
    public enum IndicatorStyle: String {
        case medium = "medium"
        case large = "large"
    }

    /// Canonical attribute names declared for this component, including the shared `common` set (public metadata contract).
    public static let declaredAttributes: Set<String> = CommonAttributes.declaredAttributes.union([
        "color",
        "hidesWhenStopped",
        "indicatorStyle",
        "progress",
        "progressImage",
        "progressTintColor",
        "progressViewStyle",
        "tintColor",
        "trackImage",
        "trackTintColor",
    ])

    /// Alias spelling → canonical attribute name (merged with common). Alias
    /// spellings that are also declared attributes keep their own
    /// entry and are not redirected.
    public static let aliasMap: [String: String] = [:]

    /// True when `key` is a declared canonical name or alias spelling.
    public static func isDeclared(_ key: String) -> Bool {
        return declaredAttributes.contains(key) || aliasMap[key] != nil
    }

    /// Attributes shared across all components.
    public let common: CommonAttributes

    /// ActivityIndicator color, distinct from tintColor for historical reasons.
    public let color: String?

    /// ActivityIndicator: auto-hide when stopped.
    public let hidesWhenStopped: Bool?

    /// ActivityIndicator size style.
    public let indicatorStyle: AttrEnum<IndicatorStyle>?

    /// Progress value (0-1, can be binding)
    public let progress: AttrValue<Double>?

    /// Custom progress image
    public let progressImage: String?

    /// Progress portion color - hex string or color name from colors.json (binding supported)
    public let progressTintColor: AttrValue<String>?

    /// Progress view style: default, bar
    public let progressViewStyle: String?

    /// Tint color - hex string or color name from colors.json
    public let tintColor: String?

    /// Custom track image
    public let trackImage: String?

    /// Track portion color - hex string or color name from colors.json (binding supported)
    public let trackTintColor: AttrValue<String>?

    /// Pass `canonicalOnly: true` for L1-normalized input —
    /// alias fallback is then disabled.
    public init(json: [String: Any], canonicalOnly: Bool = false) {
        self.common = CommonAttributes(json: json, canonicalOnly: canonicalOnly)
        self.color = AttrCoerce.string(AttrCoerce.lookup(json, "color"))
        self.hidesWhenStopped = AttrCoerce.boolean(AttrCoerce.lookup(json, "hidesWhenStopped"))
        self.indicatorStyle = Self.parseIndicatorStyle(AttrCoerce.lookup(json, "indicatorStyle"))
        self.progress = AttrCoerce.attrValue(AttrCoerce.lookup(json, "progress"), AttrCoerce.number)
        self.progressImage = AttrCoerce.string(AttrCoerce.lookup(json, "progressImage"))
        self.progressTintColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "progressTintColor"), AttrCoerce.string)
        self.progressViewStyle = AttrCoerce.string(AttrCoerce.lookup(json, "progressViewStyle"))
        self.tintColor = AttrCoerce.string(AttrCoerce.lookup(json, "tintColor"))
        self.trackImage = AttrCoerce.string(AttrCoerce.lookup(json, "trackImage"))
        self.trackTintColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "trackTintColor"), AttrCoerce.string)
    }

    private static func parseIndicatorStyle(_ raw: Any?) -> AttrEnum<IndicatorStyle>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "medium": return .known(IndicatorStyle.medium)
            case "large": return .known(IndicatorStyle.large)
            default: break
            }
        }
        AttrCodegenWarnings.emit("Progress.indicatorStyle: unknown enum value '\(raw)'")
        return .unknown(raw)
    }
}
