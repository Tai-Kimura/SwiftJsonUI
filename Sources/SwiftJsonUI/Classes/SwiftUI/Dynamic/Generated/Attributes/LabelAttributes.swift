// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang swift

import Foundation

/// Typed attribute extraction for the `Label` component.
/// Shared attributes are available via `common`.
public struct LabelAttributes {
    public enum LineBreakMode: String {
        case char = "Char"
        case clip = "Clip"
        case word = "Word"
        case head = "Head"
        case middle = "Middle"
        case tail = "Tail"
    }

    public enum TextAlign: String {
        case left = "Left"
        case center = "Center"
        case right = "Right"
    }

    public enum TextTransform: String {
        case none = "none"
        case capitalize = "capitalize"
        case uppercase = "uppercase"
        case lowercase = "lowercase"
    }

    /// Canonical attribute names declared for this component, including the shared `common` set (public metadata contract).
    public static let declaredAttributes: Set<String> = CommonAttributes.declaredAttributes.union([
        "autoShrink",
        "edgeInset",
        "font",
        "fontColor",
        "fontFamily",
        "fontSize",
        "fontWeight",
        "highlightAttributes",
        "highlightColor",
        "hint",
        "hintAttributes",
        "hintColor",
        "lineBreakMode",
        "lineHeight",
        "lineHeightMultiple",
        "lineSpacing",
        "lines",
        "linkable",
        "minimumScaleFactor",
        "partialAttributes",
        "placeholder",
        "selected",
        "strikethrough",
        "text",
        "textAlign",
        "textShadow",
        "textTransform",
        "underline",
    ])

    /// Alias spelling → canonical attribute name (merged with common). Alias
    /// spellings that are also declared attributes keep their own
    /// entry and are not redirected.
    public static let aliasMap: [String: String] = [:]

    /// True when `key` is a declared canonical name or alias spelling.
    public static func isDeclared(_ key: String) -> Bool {
        return declaredAttributes.contains(key) || aliasMap[key] != nil
    }

    /// Attributes shared across all components.
    public let common: CommonAttributes

    /// Enable auto-shrink
    public let autoShrink: Bool?

    /// Edge insets as array or pipe-separated string [accepts: array | string; DEPRECATED: Compose Text has no edgeInset; use padding* instead.]
    public let edgeInset: Any?

    /// Font weight name (regular/medium/semibold/bold/...) or font name. Passed as the `weight` field of `FontSpec` to `Configuration.Font.fontProvider`. Can be a data binding.
    public let font: AttrValue<String>?

    /// Font color - hex string or color name from colors.json (can be data binding)
    public let fontColor: AttrValue<String>?

    /// Font family name (e.g., 'Noto Sans JP'). Passed as the `family` field of `FontSpec` to `Configuration.Font.fontProvider`. Coexists with `font` (weight) and `fontSize` which are passed in the same FontSpec.
    public let fontFamily: AttrValue<String>?

    /// Font size. Passed as the `size` field of `FontSpec` to `Configuration.Font.fontProvider`. Can be a data binding.
    public let fontSize: AttrValue<Double>?

    /// Font weight (e.g., 'bold', 'semibold', '500', 600) [accepts: string | number]
    public let fontWeight: Any?

    /// Highlight styling attributes when selected
    public let highlightAttributes: [String: Any]?

    /// Font color when selected - hex string or color name from colors.json (binding supported)
    public let highlightColor: AttrValue<String>?

    /// Placeholder text when empty (primary)
    public let hint: String?

    /// Hint text styling attributes
    public let hintAttributes: [String: Any]?

    /// Hint text color - hex string or color name from colors.json (binding supported)
    public let hintColor: AttrValue<String>?

    /// Truncation mode
    public let lineBreakMode: AttrEnum<LineBreakMode>?

    /// Line height
    public let lineHeight: Double?

    /// Line height multiplier (binding supported)
    public let lineHeightMultiple: AttrValue<Double>?

    /// Line spacing (binding supported)
    public let lineSpacing: AttrValue<Double>?

    /// Maximum number of lines (0 for unlimited) (binding supported)
    public let lines: AttrValue<Double>?

    /// Enable URL/phone detection
    public let linkable: AttrValue<Bool>?

    /// Minimum scale factor for auto-shrink (binding supported)
    public let minimumScaleFactor: AttrValue<Double>?

    /// Partial text styling
    public let partialAttributes: [Any]?

    /// Placeholder text when empty (alias for hint)
    public let placeholder: String?

    /// Selected state (binding supported)
    public let selected: AttrValue<Bool>?

    /// Strikethrough styling (boolean for simple, object for styled) [accepts: boolean | object]
    public let strikethrough: Any?

    /// Text content (can be data binding, supports interpolation)
    public let text: AttrValue<String>?

    /// Text alignment (binding supported)
    public let textAlign: AttrValue<AttrEnum<TextAlign>>?

    /// Text shadow configuration [accepts: string | object]
    public let textShadow: Any?

    /// Text transformation
    public let textTransform: AttrEnum<TextTransform>?

    /// Underline styling (boolean for simple, object for styled) [accepts: boolean | object | array]
    public let underline: Any?

    /// Pass `canonicalOnly: true` for L1-normalized input —
    /// alias fallback is then disabled.
    public init(json: [String: Any], canonicalOnly: Bool = false) {
        self.common = CommonAttributes(json: json, canonicalOnly: canonicalOnly)
        self.autoShrink = AttrCoerce.boolean(AttrCoerce.lookup(json, "autoShrink"))
        self.edgeInset = AttrCoerce.any(AttrCoerce.lookup(json, "edgeInset"))
        self.font = AttrCoerce.attrValue(AttrCoerce.lookup(json, "font"), AttrCoerce.string)
        self.fontColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "fontColor"), AttrCoerce.string)
        self.fontFamily = AttrCoerce.attrValue(AttrCoerce.lookup(json, "fontFamily"), AttrCoerce.string)
        self.fontSize = AttrCoerce.attrValue(AttrCoerce.lookup(json, "fontSize"), AttrCoerce.number)
        self.fontWeight = AttrCoerce.any(AttrCoerce.lookup(json, "fontWeight"))
        self.highlightAttributes = AttrCoerce.object(AttrCoerce.lookup(json, "highlightAttributes"))
        self.highlightColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "highlightColor"), AttrCoerce.string)
        self.hint = AttrCoerce.string(AttrCoerce.lookup(json, "hint"))
        self.hintAttributes = AttrCoerce.object(AttrCoerce.lookup(json, "hintAttributes"))
        self.hintColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "hintColor"), AttrCoerce.string)
        self.lineBreakMode = Self.parseLineBreakMode(AttrCoerce.lookup(json, "lineBreakMode"))
        self.lineHeight = AttrCoerce.number(AttrCoerce.lookup(json, "lineHeight"))
        self.lineHeightMultiple = AttrCoerce.attrValue(AttrCoerce.lookup(json, "lineHeightMultiple"), AttrCoerce.number)
        self.lineSpacing = AttrCoerce.attrValue(AttrCoerce.lookup(json, "lineSpacing"), AttrCoerce.number)
        self.lines = AttrCoerce.attrValue(AttrCoerce.lookup(json, "lines"), AttrCoerce.number)
        self.linkable = AttrCoerce.attrValue(AttrCoerce.lookup(json, "linkable"), AttrCoerce.boolean)
        self.minimumScaleFactor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "minimumScaleFactor"), AttrCoerce.number)
        self.partialAttributes = AttrCoerce.array(AttrCoerce.lookup(json, "partialAttributes"))
        self.placeholder = AttrCoerce.string(AttrCoerce.lookup(json, "placeholder"))
        self.selected = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selected"), AttrCoerce.boolean)
        self.strikethrough = AttrCoerce.any(AttrCoerce.lookup(json, "strikethrough"))
        self.text = AttrCoerce.attrValue(AttrCoerce.lookup(json, "text"), AttrCoerce.string)
        self.textAlign = AttrCoerce.attrValue(AttrCoerce.lookup(json, "textAlign"), { Self.parseTextAlign($0) })
        self.textShadow = AttrCoerce.any(AttrCoerce.lookup(json, "textShadow"))
        self.textTransform = Self.parseTextTransform(AttrCoerce.lookup(json, "textTransform"))
        self.underline = AttrCoerce.any(AttrCoerce.lookup(json, "underline"))
    }

    private static func parseLineBreakMode(_ raw: Any?) -> AttrEnum<LineBreakMode>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "char": return .known(LineBreakMode.char)
            case "clip": return .known(LineBreakMode.clip)
            case "word": return .known(LineBreakMode.word)
            case "head": return .known(LineBreakMode.head)
            case "middle": return .known(LineBreakMode.middle)
            case "tail": return .known(LineBreakMode.tail)
            default: break
            }
        }
        AttrCodegenWarnings.emit("Label.lineBreakMode: unknown enum value '\(raw)'")
        return .unknown(raw)
    }

    private static func parseTextAlign(_ raw: Any?) -> AttrEnum<TextAlign>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "left": return .known(TextAlign.left)
            case "center": return .known(TextAlign.center)
            case "right": return .known(TextAlign.right)
            default: break
            }
        }
        AttrCodegenWarnings.emit("Label.textAlign: unknown enum value '\(raw)'")
        return .unknown(raw)
    }

    private static func parseTextTransform(_ raw: Any?) -> AttrEnum<TextTransform>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "none": return .known(TextTransform.none)
            case "capitalize": return .known(TextTransform.capitalize)
            case "uppercase": return .known(TextTransform.uppercase)
            case "lowercase": return .known(TextTransform.lowercase)
            default: break
            }
        }
        AttrCodegenWarnings.emit("Label.textTransform: unknown enum value '\(raw)'")
        return .unknown(raw)
    }
}
