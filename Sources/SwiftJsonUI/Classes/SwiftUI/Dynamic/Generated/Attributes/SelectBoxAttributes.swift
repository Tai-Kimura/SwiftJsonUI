// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang swift

import Foundation

/// Typed attribute extraction for the `SelectBox` component.
/// Shared attributes are available via `common`.
public struct SelectBoxAttributes {
    public enum ColorScheme: String {
        case light = "light"
        case dark = "dark"
    }

    public enum DatePickerMode: String {
        case date = "date"
        case time = "time"
        case datetime = "datetime"
        case dateAndTime = "dateAndTime"
        case countDown = "countDown"
    }

    public enum DatePickerStyle: String {
        case automatic = "automatic"
        case wheel = "wheel"
        case wheels = "wheels"
        case compact = "compact"
        case graphical = "graphical"
        case inline = "inline"
    }

    public enum SelectItemType: String {
        case normal = "Normal"
        case date = "Date"
    }

    /// Canonical attribute names declared for this component, including the shared `common` set (public metadata contract).
    public static let declaredAttributes: Set<String> = CommonAttributes.declaredAttributes.union([
        "canBack",
        "caretAttributes",
        "colorScheme",
        "datePickerMode",
        "datePickerStyle",
        "dateStringFormat",
        "dividerAttributes",
        "font",
        "fontColor",
        "fontSize",
        "hint",
        "hintColor",
        "inView",
        "includePromptWhenDataBinding",
        "items",
        "labelAttributes",
        "maximumDate",
        "minimumDate",
        "minuteInterval",
        "multiple",
        "onValueChange",
        "onValueChanged",
        "placeholder",
        "prompt",
        "referenceView",
        "selectItemType",
        "selectedDate",
        "selectedIndex",
        "selectedItem",
        "selectedValue",
        "size",
        "text",
    ])

    /// Alias spelling → canonical attribute name (merged with common). Alias
    /// spellings that are also declared attributes keep their own
    /// entry and are not redirected.
    public static let aliasMap: [String: String] = [:]

    /// True when `key` is a declared canonical name or alias spelling.
    public static func isDeclared(_ key: String) -> Bool {
        return declaredAttributes.contains(key) || aliasMap[key] != nil
    }

    /// Attributes shared across all components.
    public let common: CommonAttributes

    /// Show back button
    public let canBack: Bool?

    /// Caret/arrow styling attributes
    public let caretAttributes: [String: Any]?

    /// Color scheme for native date picker icon visibility (set 'dark' for dark backgrounds)
    public let colorScheme: AttrEnum<ColorScheme>?

    /// Date picker mode
    public let datePickerMode: AttrEnum<DatePickerMode>?

    /// Date picker style
    public let datePickerStyle: AttrEnum<DatePickerStyle>?

    /// Date string format
    public let dateStringFormat: String?

    /// Divider styling attributes
    public let dividerAttributes: [String: Any]?

    /// Font name
    public let font: String?

    /// Font color - hex string or color name from colors.json
    public let fontColor: String?

    /// Font size
    public let fontSize: Double?

    /// Hint text (alias for prompt)
    public let hint: String?

    /// Hint text color - hex string or color name from colors.json (binding supported)
    public let hintColor: AttrValue<String>?

    /// Display picker inside view
    public let inView: String?

    /// Include prompt in binding
    public let includePromptWhenDataBinding: Bool?

    /// List of items for normal picker (can be template variable)
    public let items: AttrValue<[Any]>?

    /// Label styling attributes
    public let labelAttributes: [String: Any]?

    /// Maximum date (yyyy-MM-dd format, binding supported)
    public let maximumDate: AttrValue<String>?

    /// Minimum date (yyyy-MM-dd format, binding supported)
    public let minimumDate: AttrValue<String>?

    /// Minute interval for time picker
    public let minuteInterval: Double?

    /// Allow multiple selection
    public let multiple: Bool?

    /// Value change handler - binding only (@{functionName})
    public let onValueChange: AttrValue<Any>?

    /// Value change handler (alias for onValueChange) - binding only (@{functionName})
    public let onValueChanged: AttrValue<Any>?

    /// Placeholder text (alias for prompt)
    public let placeholder: String?

    /// Prompt text (primary)
    public let prompt: String?

    /// Reference view for picker
    public let referenceView: String?

    /// Select item type
    public let selectItemType: AttrEnum<SelectItemType>?

    /// Initial selected date (yyyy-MM-dd format, two-way binding)
    public let selectedDate: AttrValue<String>?

    /// Initial selected index for normal picker (two-way binding) [binding: two-way]
    public let selectedIndex: AttrValue<Double>?

    /// Selected item value (two-way binding)
    public let selectedItem: AttrValue<String>?

    /// Selected value (binding for two-way)
    public let selectedValue: AttrValue<String>?

    /// Number of visible options
    public let size: Double?

    /// Display text (binding supported)
    public let text: AttrValue<String>?

    /// Pass `canonicalOnly: true` for L1-normalized input —
    /// alias fallback is then disabled.
    public init(json: [String: Any], canonicalOnly: Bool = false) {
        self.common = CommonAttributes(json: json, canonicalOnly: canonicalOnly)
        self.canBack = AttrCoerce.boolean(AttrCoerce.lookup(json, "canBack"))
        self.caretAttributes = AttrCoerce.object(AttrCoerce.lookup(json, "caretAttributes"))
        self.colorScheme = Self.parseColorScheme(AttrCoerce.lookup(json, "colorScheme"))
        self.datePickerMode = Self.parseDatePickerMode(AttrCoerce.lookup(json, "datePickerMode"))
        self.datePickerStyle = Self.parseDatePickerStyle(AttrCoerce.lookup(json, "datePickerStyle"))
        self.dateStringFormat = AttrCoerce.string(AttrCoerce.lookup(json, "dateStringFormat"))
        self.dividerAttributes = AttrCoerce.object(AttrCoerce.lookup(json, "dividerAttributes"))
        self.font = AttrCoerce.string(AttrCoerce.lookup(json, "font"))
        self.fontColor = AttrCoerce.string(AttrCoerce.lookup(json, "fontColor"))
        self.fontSize = AttrCoerce.number(AttrCoerce.lookup(json, "fontSize"))
        self.hint = AttrCoerce.string(AttrCoerce.lookup(json, "hint"))
        self.hintColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "hintColor"), AttrCoerce.string)
        self.inView = AttrCoerce.string(AttrCoerce.lookup(json, "inView"))
        self.includePromptWhenDataBinding = AttrCoerce.boolean(AttrCoerce.lookup(json, "includePromptWhenDataBinding"))
        self.items = AttrCoerce.attrValue(AttrCoerce.lookup(json, "items"), AttrCoerce.array)
        self.labelAttributes = AttrCoerce.object(AttrCoerce.lookup(json, "labelAttributes"))
        self.maximumDate = AttrCoerce.attrValue(AttrCoerce.lookup(json, "maximumDate"), AttrCoerce.string)
        self.minimumDate = AttrCoerce.attrValue(AttrCoerce.lookup(json, "minimumDate"), AttrCoerce.string)
        self.minuteInterval = AttrCoerce.number(AttrCoerce.lookup(json, "minuteInterval"))
        self.multiple = AttrCoerce.boolean(AttrCoerce.lookup(json, "multiple"))
        self.onValueChange = AttrCoerce.bindingValue(AttrCoerce.lookup(json, "onValueChange"))
        self.onValueChanged = AttrCoerce.bindingValue(AttrCoerce.lookup(json, "onValueChanged"))
        self.placeholder = AttrCoerce.string(AttrCoerce.lookup(json, "placeholder"))
        self.prompt = AttrCoerce.string(AttrCoerce.lookup(json, "prompt"))
        self.referenceView = AttrCoerce.string(AttrCoerce.lookup(json, "referenceView"))
        self.selectItemType = Self.parseSelectItemType(AttrCoerce.lookup(json, "selectItemType"))
        self.selectedDate = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selectedDate"), AttrCoerce.string)
        self.selectedIndex = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selectedIndex"), AttrCoerce.number)
        self.selectedItem = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selectedItem"), AttrCoerce.string)
        self.selectedValue = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selectedValue"), AttrCoerce.string)
        self.size = AttrCoerce.number(AttrCoerce.lookup(json, "size"))
        self.text = AttrCoerce.attrValue(AttrCoerce.lookup(json, "text"), AttrCoerce.string)
    }

    private static func parseColorScheme(_ raw: Any?) -> AttrEnum<ColorScheme>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "light": return .known(ColorScheme.light)
            case "dark": return .known(ColorScheme.dark)
            default: break
            }
        }
        AttrCodegenWarnings.emit("SelectBox.colorScheme: unknown enum value '\(raw)'")
        return .unknown(raw)
    }

    private static func parseDatePickerMode(_ raw: Any?) -> AttrEnum<DatePickerMode>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "date": return .known(DatePickerMode.date)
            case "time": return .known(DatePickerMode.time)
            case "datetime": return .known(DatePickerMode.datetime)
            case "dateandtime": return .known(DatePickerMode.dateAndTime)
            case "countdown": return .known(DatePickerMode.countDown)
            default: break
            }
        }
        AttrCodegenWarnings.emit("SelectBox.datePickerMode: unknown enum value '\(raw)'")
        return .unknown(raw)
    }

    private static func parseDatePickerStyle(_ raw: Any?) -> AttrEnum<DatePickerStyle>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "automatic": return .known(DatePickerStyle.automatic)
            case "wheel": return .known(DatePickerStyle.wheel)
            case "wheels": return .known(DatePickerStyle.wheels)
            case "compact": return .known(DatePickerStyle.compact)
            case "graphical": return .known(DatePickerStyle.graphical)
            case "inline": return .known(DatePickerStyle.inline)
            default: break
            }
        }
        AttrCodegenWarnings.emit("SelectBox.datePickerStyle: unknown enum value '\(raw)'")
        return .unknown(raw)
    }

    private static func parseSelectItemType(_ raw: Any?) -> AttrEnum<SelectItemType>? {
        guard let raw = raw, !(raw is NSNull) else { return nil }
        if let s = raw as? String {
            switch s.lowercased() {
            case "normal": return .known(SelectItemType.normal)
            case "date": return .known(SelectItemType.date)
            default: break
            }
        }
        AttrCodegenWarnings.emit("SelectBox.selectItemType: unknown enum value '\(raw)'")
        return .unknown(raw)
    }
}
