// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang swift

import Foundation

/// Typed attribute extraction for the `TabView` component.
/// Shared attributes are available via `common`.
/// Overrides the common definition of: `tintColor` (use the property on this struct).
public struct TabViewAttributes {
    /// Canonical attribute names declared for this component, including the shared `common` set (public metadata contract).
    public static let declaredAttributes: Set<String> = CommonAttributes.declaredAttributes.union([
        "onValueChange",
        "selectedIndex",
        "showLabels",
        "tabBarBackground",
        "tabs",
        "tintColor",
        "unselectedColor",
    ])

    /// Alias spelling → canonical attribute name (merged with common). Alias
    /// spellings that are also declared attributes keep their own
    /// entry and are not redirected.
    public static let aliasMap: [String: String] = [
        "alpha": "opacity",
        "onPageChanged": "onValueChange",
        "onTabChange": "onValueChange",
        "selectedTabIndex": "selectedIndex",
    ]

    /// True when `key` is a declared canonical name or alias spelling.
    public static func isDeclared(_ key: String) -> Bool {
        return declaredAttributes.contains(key) || aliasMap[key] != nil
    }

    /// Attributes shared across all components.
    public let common: CommonAttributes

    /// Tab/page selection change handler. Canonical; prefer over onTabChange/onPageChanged. [aliases: onTabChange, onPageChanged; binding: one-way]
    public let onValueChange: AttrValue<String>?

    /// Currently selected tab index (binding for two-way) [aliases: selectedTabIndex; binding: two-way]
    public let selectedIndex: AttrValue<Double>?

    /// Whether to show tab labels (default: true) [default: true]
    public let showLabels: Bool?

    /// Tab bar background color - hex string or color name from colors.json (binding supported)
    public let tabBarBackground: AttrValue<String>?

    /// Array of tab configurations [required]
    public let tabs: [Any]?

    /// Selected tab tint color - hex string or color name from colors.json (binding supported)
    public let tintColor: AttrValue<String>?

    /// Unselected tab color - hex string or color name from colors.json (binding supported). SCOPE: this is the colour of an unselected TAB ITEM, so it only concerns TabView used as a tab bar. A TabView carrying .tabViewStyle(.page) is a PAGER and has no tab items at all, so nothing here applies to it. iOS 26 AND LATER DO NOT HONOUR THIS, and there is no replacement: SwiftUI exposes no per-item tint for a tab bar (the whole tab/toolbar modifier surface was enumerated from the iOS 27 SDK swiftinterface - tabBarMinimizeBehavior, tabViewCustomization, tabViewSearchActivation and eleven toolbar* modifiers, none of which reaches an item), so the iOS render path routes through the UIKit appearance proxy UITabBar.appearance().unselectedItemTintColor. Measured 2026-09-15 on iPhone 16 Pro / iOS 26.5: the fixture renders 0 differing pixels from its control while tabBarBackground renders 223110 and tintColor 4553 on the same capture. The proxy is not being cleared - reading it back immediately after the set and again one runloop later both return the assigned colour - iOS 26 simply ignores it for item tints, having replaced the tab bar. Three replacement routes were rendered and each measured 0: the legacy property, UITabBarAppearance item colours (stackedLayoutAppearance / inlineLayoutAppearance / compactInlineLayoutAppearance, assigned to standardAppearance and scrollEdgeAppearance) in onAppear, and the same set before the view is constructed; a build control injecting a red backgroundColor on that same path moved 223110 pixels, so the code demonstrably ran. NOT a v10.22.0 regression: conformance run 34849471695 shows it on iOS 26 before that tag existed, and the iOS 18 runs before it do not. The sibling keys are unaffected - tintColor, tabBarBackground (which also has a SwiftUI route) and the Slider / Switch / Segment appearance proxies are all still active on iOS 26, so this is one key, not the proxy as a whole. android and web are unaffected and still honour it.
    public let unselectedColor: AttrValue<String>?

    /// Pass `canonicalOnly: true` for L1-normalized input —
    /// alias fallback is then disabled.
    public init(json: [String: Any], canonicalOnly: Bool = false) {
        self.common = CommonAttributes(json: json, canonicalOnly: canonicalOnly)
        self.onValueChange = AttrCoerce.attrValue(AttrCoerce.lookup(json, "onValueChange", ["onTabChange", "onPageChanged"], canonicalOnly: canonicalOnly), AttrCoerce.string)
        self.selectedIndex = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selectedIndex", ["selectedTabIndex"], canonicalOnly: canonicalOnly), AttrCoerce.number)
        self.showLabels = AttrCoerce.boolean(AttrCoerce.lookup(json, "showLabels"))
        self.tabBarBackground = AttrCoerce.attrValue(AttrCoerce.lookup(json, "tabBarBackground"), AttrCoerce.string)
        self.tabs = AttrCoerce.array(AttrCoerce.lookup(json, "tabs"))
        self.tintColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "tintColor"), AttrCoerce.string)
        self.unselectedColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "unselectedColor"), AttrCoerce.string)
    }
}
